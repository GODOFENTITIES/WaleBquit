import { Bot } from 'lucide-react';

export function Logo() {
  return (
    <div className="flex items-center justify-center size-16 bg-primary/10 rounded-full border-4 border-primary/20">
      <Bot className="size-8 text-primary" />
    </div>
  );
}
