import { Bot } from 'lucide-react';

export function Logo() {
  return (
    <div className="flex items-center justify-center size-12 bg-primary/10 rounded-lg border-2 border-primary/20">
      <Bot className="size-6 text-primary" />
    </div>
  );
}
